using NUnit.Framework;
using UnityEngine;

public class EnemyLockedDoor : MonoBehaviour
{

    // Update is called once per frame
    void Update()
    {
        //Basically, we check if the amount of enemies with a certain tag is = 0, and if so, we disable the barrier.

    }
}
